import { AfterContentChecked, EventEmitter, OnChanges, OnDestroy, OnInit, QueryList, SimpleChanges } from '@angular/core';
import { KanbanService } from '../../internal/kanban.service';
import { CardDefDirective } from '../card-def.directive';
import { ColumnDefDirective } from '../column-def.directive';
import { DataSorter, DataFilter, DropValidator, KanbanColumnDefWithTemplate, KanbanData, KanbanDrop } from '../kanban.models';
export declare class BoardComponent<T extends KanbanData = KanbanData> implements AfterContentChecked, OnDestroy, OnInit, OnChanges {
    private kanban;
    private readonly destroy$;
    columns: KanbanColumnDefWithTemplate[];
    cardDef: CardDefDirective;
    columnDefs: QueryList<ColumnDefDirective>;
    data: KanbanData[];
    visibleColumns: string[];
    dropValidator: DropValidator;
    columnSorter: DataSorter;
    dataFilter: DataFilter;
    readonly cardDrop: EventEmitter<KanbanDrop<T>>;
    private _updateColumns;
    constructor(kanban: KanbanService);
    ngOnInit(): void;
    ngOnDestroy(): void;
    ngAfterContentChecked(): void;
    ngOnChanges(changes: SimpleChanges): void;
}
