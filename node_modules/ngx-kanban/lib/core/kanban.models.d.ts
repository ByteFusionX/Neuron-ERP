import { TemplateRef } from '@angular/core';
export interface KanbanLabel {
    readonly id: string;
    readonly name?: string;
}
export interface KanbanColumnDef {
    readonly name: string;
    readonly labels: KanbanLabel[];
}
export interface KanbanColumnDefWithTemplate extends KanbanColumnDef {
    readonly cardTemplate: TemplateRef<any>;
}
export interface KanbanData {
    readonly id: string;
    readonly labelId: string;
}
export interface KanbanDrop<T extends KanbanData = KanbanData> {
    readonly data: T;
    readonly newLabelId: string;
}
export declare function performKanbanDrop<T extends KanbanData = KanbanData>(items: T[], drop: KanbanDrop<T>): T[];
export declare type DropValidator = (drop: KanbanDrop) => boolean;
export declare function defaultDropValidator(_: KanbanDrop): boolean;
export declare type DataSorter = (a: KanbanData, b: KanbanData) => number;
export declare function defaultDataSorter(a: KanbanData, b: KanbanData): number;
export declare type DataFilter = (data: KanbanData) => boolean;
export declare const defaultDataFilter: DataFilter;
