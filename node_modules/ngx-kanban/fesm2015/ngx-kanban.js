import { DragDropModule } from '@angular/cdk/drag-drop';
import { Component, ChangeDetectionStrategy, Directive, TemplateRef, ContentChild, Input, ɵɵdefineInjectable, Injectable, EventEmitter, ContentChildren, Output, Pipe, NgModule } from '@angular/core';
import { CommonModule } from '@angular/common';
import { BehaviorSubject, Subject } from 'rxjs';
import { map, takeUntil, filter, tap } from 'rxjs/operators';

class CardComponent {
}
CardComponent.decorators = [
    { type: Component, args: [{
                selector: 'kanban-card',
                template: "<div class=\"header\">\n  <h3>\n    <ng-content select=\"[title]\"></ng-content>\n  </h3>\n</div>\n\n<div class=\"content\">\n  <ng-content select=\"[content]\"></ng-content>\n</div>\n\n<div class=\"footer\">\n  <ng-content select=\"[footer]\"></ng-content>\n</div>\n",
                changeDetection: ChangeDetectionStrategy.OnPush,
                styles: [":host{background:#fff;border-radius:4px;box-shadow:0 3px 1px -2px rgba(0,0,0,.2),0 2px 2px 0 rgba(0,0,0,.14),0 1px 5px 0 rgba(0,0,0,.12);display:flex;flex-direction:column;padding:8px}h3{margin:0}"]
            },] }
];

class CardDefDirective {
    constructor(template) {
        this.template = template;
    }
}
CardDefDirective.decorators = [
    { type: Directive, args: [{
                selector: '[kanbanCardDef]',
            },] }
];
CardDefDirective.ctorParameters = () => [
    { type: TemplateRef }
];

class ColumnDefDirective {
    constructor() {
        this.labels = [];
    }
    get cardTemplate() {
        var _a;
        return (_a = this.card) === null || _a === void 0 ? void 0 : _a.template;
    }
}
ColumnDefDirective.decorators = [
    { type: Directive, args: [{
                selector: '[kanbanColumnDef]',
            },] }
];
ColumnDefDirective.propDecorators = {
    card: [{ type: ContentChild, args: [CardDefDirective,] }],
    name: [{ type: Input, args: ['kanbanColumnDef',] }],
    labels: [{ type: Input }]
};

class ColumnHeadingComponent {
}
ColumnHeadingComponent.decorators = [
    { type: Component, args: [{
                selector: 'kanban-column-heading',
                template: "<h3>{{ column.name }} ({{ data.length }})</h3>\n",
                changeDetection: ChangeDetectionStrategy.OnPush,
                styles: [":host{align-items:center;border-bottom:1px solid #bababa;display:flex;height:100%;justify-content:center;text-align:center}h3{margin:0}"]
            },] }
];
ColumnHeadingComponent.propDecorators = {
    column: [{ type: Input }],
    data: [{ type: Input }]
};

class KanbanService {
    constructor() {
        this.draggableSubject = new BehaviorSubject(null);
        this.onDropSubject = new Subject();
        this.draggable$ = this.draggableSubject.asObservable();
        this.isDragging$ = this.draggable$.pipe(map((v) => v !== null));
        this.isNotDragging$ = this.isDragging$.pipe(map((v) => !v));
        this.onDrop$ = this.onDropSubject.asObservable();
    }
    updateDraggable(data) {
        this.draggableSubject.next(data);
    }
    dropCard(data, newLabelId) {
        this.onDropSubject.next({ data, newLabelId });
    }
}
KanbanService.ɵprov = ɵɵdefineInjectable({ factory: function KanbanService_Factory() { return new KanbanService(); }, token: KanbanService, providedIn: "root" });
KanbanService.decorators = [
    { type: Injectable, args: [{
                providedIn: 'root',
            },] }
];

function performKanbanDrop(items, drop) {
    const updatedItem = Object.assign(Object.assign({}, drop.data), { labelId: drop.newLabelId });
    return [...items.filter((e) => e.id !== drop.data.id), updatedItem];
}
function defaultDropValidator(_) {
    return true;
}
function defaultDataSorter(a, b) {
    if (a.id > b.id) {
        return 1;
    }
    if (a.id < b.id) {
        return -1;
    }
    return 0;
}
const defaultDataFilter = (data) => true;

function _hasChanged(change) {
    return !!change && change.currentValue !== change.previousValue;
}
class BoardComponent {
    constructor(kanban) {
        this.kanban = kanban;
        this.destroy$ = new Subject();
        this.columns = [];
        this.data = [];
        this.dropValidator = defaultDropValidator;
        this.columnSorter = defaultDataSorter;
        this.cardDrop = new EventEmitter();
    }
    _updateColumns() {
        var _a, _b, _c;
        const columnNames = (_c = (_a = this.visibleColumns) !== null && _a !== void 0 ? _a : (_b = this.columnDefs) === null || _b === void 0 ? void 0 : _b.map((e) => e.name)) !== null && _c !== void 0 ? _c : [];
        this.columns = columnNames
            .map((name) => this.columnDefs.find((def) => def.name === name))
            .filter((e) => e !== null)
            .map((def) => {
            var _a, _b;
            return (Object.assign(Object.assign({}, def), { cardTemplate: (_a = def.cardTemplate) !== null && _a !== void 0 ? _a : (_b = this.cardDef) === null || _b === void 0 ? void 0 : _b.template }));
        });
    }
    ngOnInit() {
        this.kanban.onDrop$
            .pipe(takeUntil(this.destroy$), filter((drop) => this.dropValidator(drop)), tap((drop) => this.cardDrop.emit(drop)))
            .subscribe();
    }
    ngOnDestroy() {
        this.destroy$.next();
    }
    ngAfterContentChecked() {
        this._updateColumns();
    }
    ngOnChanges(changes) {
        if (_hasChanged(changes.visibleColumns)) {
            this._updateColumns();
        }
    }
}
BoardComponent.decorators = [
    { type: Component, args: [{
                selector: 'kanban-board',
                template: "<ng-container cdkDropListGroup>\n  <kanban-column-layout [columns]=\"columns\" class=\"header\">\n    <kanban-column-heading\n      *kanbanContentDef=\"let column\"\n      [column]=\"column\"\n      [data]=\"data | filterByLabels: column.labels\"\n    ></kanban-column-heading>\n  </kanban-column-layout>\n\n  <kanban-column-layout [columns]=\"columns\" class=\"content\">\n    <kanban-column-content\n      *kanbanContentDef=\"let column\"\n      [column]=\"column\"\n      [data]=\"\n        data\n          | filterByPredicate: dataFilter\n          | filterByLabels: column.labels\n          | sortColumnData: columnSorter\n      \"\n    ></kanban-column-content>\n  </kanban-column-layout>\n</ng-container>\n",
                viewProviders: [KanbanService],
                styles: [".header{box-sizing:border-box;height:72px}.content{max-height:calc(100% - 72px);overflow:hidden}"]
            },] }
];
BoardComponent.ctorParameters = () => [
    { type: KanbanService }
];
BoardComponent.propDecorators = {
    cardDef: [{ type: ContentChild, args: [CardDefDirective,] }],
    columnDefs: [{ type: ContentChildren, args: [ColumnDefDirective,] }],
    data: [{ type: Input }],
    visibleColumns: [{ type: Input }],
    dropValidator: [{ type: Input }],
    columnSorter: [{ type: Input }],
    dataFilter: [{ type: Input }],
    cardDrop: [{ type: Output }]
};

class CardListComponent {
    constructor(kanban) {
        this.kanban = kanban;
    }
    onDragStarted(event) {
        this.kanban.updateDraggable(event.source.data);
    }
    onDragEnded(_) {
        this.kanban.updateDraggable(null);
    }
}
CardListComponent.decorators = [
    { type: Component, args: [{
                selector: 'kanban-card-list',
                template: "<div cdkDropList class=\"cards\">\n  <ng-container *ngFor=\"let d of data\">\n    <div\n      cdkDrag\n      class=\"card-container\"\n      [cdkDragData]=\"d\"\n      (cdkDragStarted)=\"onDragStarted($event)\"\n      (cdkDragEnded)=\"onDragEnded($event)\"\n    >\n      <div *cdkDragPlaceholder></div>\n      <ng-container\n        *ngTemplateOutlet=\"column.cardTemplate; context: { $implicit: d }\"\n      ></ng-container>\n    </div>\n  </ng-container>\n</div>\n",
                changeDetection: ChangeDetectionStrategy.OnPush,
                styles: [":host{display:block}:host::-webkit-scrollbar{background-color:#fafafa;border-radius:4px;width:4px}:host::-webkit-scrollbar-thumb{background-color:#bababa}.cards{display:flex;flex-direction:column}.cards .card-container:not(:last-child){margin-bottom:8px}.cdk-drag-animating{transition:transform .3s cubic-bezier(0,0,.2,1)}.cdk-drag-placeholder{background:#eee;border:2px dashed #bababa;border-radius:4px;margin:8px;min-height:64px;width:calc(100% - 16px)}"]
            },] }
];
CardListComponent.ctorParameters = () => [
    { type: KanbanService }
];
CardListComponent.propDecorators = {
    column: [{ type: Input }],
    data: [{ type: Input }]
};

class ColumnContentComponent {
    constructor(kanban) {
        this.kanban = kanban;
    }
}
ColumnContentComponent.decorators = [
    { type: Component, args: [{
                selector: 'kanban-column-content',
                template: "<kanban-drop-zone-list\n  [column]=\"column\"\n  [class.visible]=\"kanban.isDragging$ | async\"\n></kanban-drop-zone-list>\n\n<kanban-card-list\n  [data]=\"data\"\n  [column]=\"column\"\n  [class.visible]=\"kanban.isNotDragging$ | async\"\n></kanban-card-list>\n",
                changeDetection: ChangeDetectionStrategy.OnPush,
                styles: [":host{display:block;height:100%;margin:0 8px;position:relative}kanban-card-list,kanban-drop-zone-list{visibility:hidden}kanban-card-list.visible,kanban-drop-zone-list.visible{visibility:visible}kanban-drop-zone-list{left:0;position:absolute;right:0}kanban-card-list{height:calc(100% - 24px);margin-top:8px;overflow-y:auto;padding:8px}"]
            },] }
];
ColumnContentComponent.ctorParameters = () => [
    { type: KanbanService }
];
ColumnContentComponent.propDecorators = {
    column: [{ type: Input }],
    data: [{ type: Input }]
};

class ContentDefDirective {
    constructor(template) {
        this.template = template;
    }
}
ContentDefDirective.decorators = [
    { type: Directive, args: [{
                selector: '[kanbanContentDef]',
            },] }
];
ContentDefDirective.ctorParameters = () => [
    { type: TemplateRef }
];

/**
 * Renders a template for each column given by {@link columns}.
 * This ensures that components are aligned within their column,
 * if they are not contained within the same parent.
 * E.g. having a column header and content, but only allowing the content to scroll.
 */
class ColumnLayoutComponent {
}
ColumnLayoutComponent.decorators = [
    { type: Component, args: [{
                selector: 'kanban-column-layout',
                template: "<ng-container *ngFor=\"let c of columns\">\n  <div class=\"content\">\n    <ng-container\n      *ngTemplateOutlet=\"content.template; context: { $implicit: c }\"\n    ></ng-container>\n  </div>\n</ng-container>\n",
                changeDetection: ChangeDetectionStrategy.OnPush,
                styles: [":host{display:flex;flex-direction:row;justify-content:center}.content{flex:1}"]
            },] }
];
ColumnLayoutComponent.propDecorators = {
    content: [{ type: ContentChild, args: [ContentDefDirective,] }],
    columns: [{ type: Input }]
};

class DropZoneComponent {
    constructor(kanban) {
        this.kanban = kanban;
    }
    get name() {
        var _a, _b, _c;
        return (_b = (_a = this.label) === null || _a === void 0 ? void 0 : _a.name) !== null && _b !== void 0 ? _b : (_c = this.label) === null || _c === void 0 ? void 0 : _c.id;
    }
    onDrop(event) {
        if (event.previousContainer === event.container) {
            return;
        }
        const dropListLabelId = event.container.data.id;
        const itemLabelId = event.item.data.labelId;
        if (dropListLabelId !== itemLabelId) {
            this.kanban.dropCard(event.item.data, dropListLabelId);
        }
    }
}
DropZoneComponent.decorators = [
    { type: Component, args: [{
                selector: 'kanban-drop-zone',
                template: "<div\n  cdkDropList\n  [cdkDropListData]=\"label\"\n  (cdkDropListDropped)=\"onDrop($event)\"\n>\n  <div class=\"name\">{{ name }}</div>\n</div>\n",
                styles: [":host{display:block}[cdkDropList]{align-items:center;border:3px dashed rgba(0,0,0,.5);border-radius:4px;display:flex;height:96px;justify-content:center;transition:opacity .3s cubic-bezier(0,0,.2,1)}[cdkDropList]:not(:last-child){margin-bottom:8px}[cdkDropList].cdk-drop-list-receiving{border-color:rgba(0,0,0,.75);opacity:.6}[cdkDropList].cdk-drop-list-dragging{border-color:#000;opacity:1}[cdkDropList].cdk-drop-list-dragging .name{display:none}"]
            },] }
];
DropZoneComponent.ctorParameters = () => [
    { type: KanbanService }
];
DropZoneComponent.propDecorators = {
    label: [{ type: Input }]
};

class DropZoneListComponent {
}
DropZoneListComponent.decorators = [
    { type: Component, args: [{
                selector: 'kanban-drop-zone-list',
                template: "<kanban-drop-zone\n  *ngFor=\"let label of column.labels\"\n  [label]=\"label\"\n></kanban-drop-zone>\n",
                changeDetection: ChangeDetectionStrategy.OnPush,
                styles: ["kanban-drop-zone{background:#fff;border-radius:4px}kanban-drop-zone:not(:last-child){margin-bottom:8px}"]
            },] }
];
DropZoneListComponent.propDecorators = {
    column: [{ type: Input }]
};

class FilterByLabelsPipe {
    transform(values, labels) {
        const labelIds = labels.map((it) => it.id);
        return values.filter((it) => labelIds.includes(it.labelId));
    }
}
FilterByLabelsPipe.decorators = [
    { type: Pipe, args: [{
                name: 'filterByLabels',
            },] }
];

class SortColumnDataPipe {
    transform(data, sorter) {
        const ret = [...data];
        ret.sort(sorter);
        console.log(ret);
        return ret;
    }
}
SortColumnDataPipe.decorators = [
    { type: Pipe, args: [{
                name: 'sortColumnData',
            },] }
];

class FilterByPredicatePipe {
    transform(value, predicate) {
        return value.filter(predicate !== null && predicate !== void 0 ? predicate : (() => true));
    }
}
FilterByPredicatePipe.decorators = [
    { type: Pipe, args: [{
                name: 'filterByPredicate',
            },] }
];

class KanbanModule {
}
KanbanModule.decorators = [
    { type: NgModule, args: [{
                declarations: [
                    CardComponent,
                    CardDefDirective,
                    CardListComponent,
                    ColumnContentComponent,
                    ColumnDefDirective,
                    ColumnHeadingComponent,
                    ColumnLayoutComponent,
                    ContentDefDirective,
                    DropZoneComponent,
                    DropZoneListComponent,
                    FilterByLabelsPipe,
                    BoardComponent,
                    SortColumnDataPipe,
                    FilterByPredicatePipe,
                ],
                imports: [CommonModule, DragDropModule],
                exports: [
                    CardComponent,
                    CardDefDirective,
                    ColumnDefDirective,
                    ColumnHeadingComponent,
                    BoardComponent,
                ],
            },] }
];

/*
 * Public API Surface of ngx-kanban
 */

/**
 * Generated bundle index. Do not edit.
 */

export { BoardComponent, CardComponent, CardDefDirective, ColumnDefDirective, ColumnHeadingComponent, KanbanModule, defaultDataFilter, defaultDataSorter, defaultDropValidator, performKanbanDrop, CardListComponent as ɵa, KanbanService as ɵb, ColumnContentComponent as ɵc, ColumnLayoutComponent as ɵd, ContentDefDirective as ɵe, DropZoneComponent as ɵf, DropZoneListComponent as ɵg, FilterByLabelsPipe as ɵh, SortColumnDataPipe as ɵi, FilterByPredicatePipe as ɵj };
//# sourceMappingURL=ngx-kanban.js.map
